
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	РасширениеТипа = "";
	Параметры.Свойство("ВидОтображаемыхМД", РасширениеТипа);
	Если Не ЗначениеЗаполнено(РасширениеТипа) Тогда
		РасширениеТипа = ПараметрыСеанса.NSI_ТипВыбораМастерДанныхИзСписка;
	КонецЕсли;
	Если ЗначениеЗаполнено(РасширениеТипа) Тогда
		ВидОтображаемыхМД = Справочники.NSI_ВидыМД.НайтиПоНаименованию(РасширениеТипа);
	Иначе
		ВидОтображаемыхМД = Справочники.NSI_ВидыМД.ПустаяСсылка();
	КонецЕсли;
	ПолеВладельца = Новый ПолеКомпоновкиДанных("Владелец");
	Параметры.Отбор.Вставить("Владелец", ВидОтображаемыхМД);
КонецПроцедуры
