
Процедура ПередЗаписью(Отказ)
	Если ЭтоНовый() Тогда
		ID = Новый УникальныйИдентификатор();
		УстановитьСсылкуНового(Справочники.NSI_РолиIDM.ПолучитьСсылку(ID));
		RoleID = Строка(ID);
	КонецЕсли;
	Наименование = ОсновнаяИБ.ИмяБазы + "/" + Лев(ИмяРоли, 75);
	ДатаПоследнегоИзменения = ТекущаяДата();
КонецПроцедуры
