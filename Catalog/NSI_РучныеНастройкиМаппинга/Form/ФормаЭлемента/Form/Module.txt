
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(Параметры.ПравилоМаппинга) Тогда
		ХранилищеИсточник = Параметры.ПравилоМаппинга.Источник;
		ХранилищеПриемник = Параметры.ПравилоМаппинга.Приемник;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПравилоМаппингаПриИзменении(Элемент)
	ОбновитьХранилища();
КонецПроцедуры

&НаСервере
Процедура ОбновитьХранилища()
	ХранилищеИсточник = Объект.ПравилоМаппинга.Источник;
	ХранилищеПриемник = Объект.ПравилоМаппинга.Приемник;
КонецПроцедуры
