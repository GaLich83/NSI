
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Connector = NSI_МодульПовторногоИспользования.ПолучитьСоединениеПоИмениБазы(Параметры.БазаИБ.ИмяБазы);
	Если Connector = Неопределено Тогда
		Отказ = Истина;
	КонецЕсли;
	МассивТиповМетаданных= Параметры.СписокТиповМетаданных.ВыгрузитьЗначения();
	ДеревоХДТО = Connector.GetMetadataTypeTree(СериализаторXDTO.ЗаписатьXDTO(МассивТиповМетаданных));
	ДеревоЗнач = СериализаторXDTO.ПрочитатьXDTO(ДеревоХДТО);
	ЗначениеВРеквизитФормы(ДеревоЗнач, "ДеревоТипов");
КонецПроцедуры

&НаКлиенте
Процедура ДеревоТиповВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ОповеститьОВыборе(Элемент.ТекущиеДанные.ИмяМетаданных);
КонецПроцедуры