
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ТЗПараметров = Объект.Ссылка.СписокПараметров.Получить();
		Если Не ТЗПараметров = Неопределено Тогда
			СписокПараметров.Загрузить(ТЗПараметров);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	ТЗПараметров = РеквизитФормыВЗначение("СписокПараметров");
	ТекущийОбъект.СписокПараметров = Новый ХранилищеЗначения(ТЗПараметров, Новый СжатиеДанных(9));
КонецПроцедуры
