
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Объект = Параметры.Объект;
	ОтчетПоРеквизитам = Параметры.ОтчетПоРеквизитам;
	ОбновитьНаСервере();	
КонецПроцедуры

&НаКлиенте
Процедура Обновить(Команда)
	ОбновитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ОбновитьНаСервере()
	МассивРеквизитов = Новый Массив;
	Если ЗначениеЗаполнено(СписокРеквизитов) Тогда
		МассивРеквизитов.Добавить(СписокРеквизитов);
	КонецЕсли;
	ОтчетПоРеквизитам = NSI_Логирование.ПолучитьОтчетПоИсторииИзменений(Объект, МассивРеквизитов);
КонецПроцедуры
