
&НаКлиенте
Процедура ТелоСообщенияHTMLПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ЭлементHTML = ДанныеСобытия.Anchor;
	Если ЭлементHTML = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Предупреждение("Переход по ссылкам в режиме предварительного просмотра писем недоступен");
	
КонецПроцедуры
