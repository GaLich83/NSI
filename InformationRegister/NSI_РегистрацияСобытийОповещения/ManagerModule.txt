Функция ПолучитьТаблицуОповещений(КлючЗаписи) Экспорт
	Менеджер = РегистрыСведений.NSI_РегистрацияСобытийОповещения.СоздатьМенеджерЗаписи();	
	ЗаполнитьЗначенияСвойств(Менеджер, КлючЗаписи);
	Менеджер.Прочитать();
	ПараметрыОповещения = Менеджер.ПараметрыОповещения.Получить();
	Сообщить("Адресат");
	Справочники.ШаблоныОповещений.ВычислениеЗначения(КлючЗаписи.ШаблонОповещения.АлгоритмВычисленияАдресата, ПараметрыОповещения, Истина);
	Сообщить("Вложение");
	Справочники.ШаблоныОповещений.ВычислениеЗначения(КлючЗаписи.ШаблонОповещения.АлгоритмФормированияВложений, ПараметрыОповещения, Истина);
	Сообщить("Тема");
	Справочники.ШаблоныОповещений.ВычислениеЗначения(КлючЗаписи.ШаблонОповещения.ТемаСообщения, ПараметрыОповещения, Истина);
	Сообщить("Тело");
	Справочники.ШаблоныОповещений.ВычислениеЗначения(КлючЗаписи.ШаблонОповещения.ТелоСообщения, ПараметрыОповещения, Истина);
КонецФункции

Функция ВывестиЗначение(Значение)
	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Ц = 0;
		Сообщить("  Массив:");
		Для Каждого Строк Из Значение Цикл
			Сообщить("     "+Ц+": "+Строк);
			Ц = Ц+1;
		КонецЦикла;
	Иначе
		Сообщить("    "+Значение);
	КонецЕсли;
КонецФункции