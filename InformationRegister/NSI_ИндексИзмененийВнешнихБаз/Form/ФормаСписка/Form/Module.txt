
&НаКлиенте
Процедура РеиндексироватьЗаписи(Команда)
	РеиндексироватьЗаписиНаСервере();
КонецПроцедуры

&НаСервере
Процедура РеиндексироватьЗаписиНаСервере()
	Набор = РегистрыСведений.NSI_ИндексИзмененийВнешнихБаз.СоздатьНаборЗаписей();
	Набор.Записать();
	Выборка = Справочники.NSI_ИзмененияОбъектовВнешнихБаз.Выбрать();
	Пока Выборка.Следующий() Цикл
		Об = Выборка.ПолучитьОбъект();
		Об.Записать();
	КонецЦикла;
КонецПроцедуры

