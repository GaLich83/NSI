
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Список = Элементы.URL.СписокВыбора;
	Список.Добавить("http://");
	Список.Добавить("file://");
	Список.Добавить("mailto:");
	Список.Добавить("#");
КонецПроцедуры

&НаКлиенте
Процедура URLНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	Диалог.ПолноеИмяФайла = URL;
	Диалог.Заголовок = "Выберите файл";
	Диалог.Фильтр = "Все файлы|*.*";

	Если Диалог.Выбрать() Тогда
		URL = Диалог.ПолноеИмяФайла;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ЗакрытьПоOK(Команда)
	Если URL <> "" Тогда
		Закрыть(URL);
	КонецЕсли;
КонецПроцедуры

